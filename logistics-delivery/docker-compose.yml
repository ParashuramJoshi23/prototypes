version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./logistics.db:/app/logistics.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
