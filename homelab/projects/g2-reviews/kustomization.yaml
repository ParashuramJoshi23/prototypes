apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The Secret (g2-reviews-secrets, holds DATABASE_URL) is created by
# scripts/bootstrap-project.sh, so it is intentionally not listed here.
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
