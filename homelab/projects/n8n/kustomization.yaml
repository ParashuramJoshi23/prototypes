apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secret 'n8n-secrets' (DB_POSTGRESDB_PASSWORD + N8N_ENCRYPTION_KEY) is created
# out-of-band from the bootstrap-generated DB password, so it is not listed here.
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
