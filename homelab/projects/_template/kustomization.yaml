apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Per-project app wiring. The Secret is created out-of-band by
# scripts/bootstrap-project.sh, so it is NOT listed here.
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
